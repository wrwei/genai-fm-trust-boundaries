# Experiment record maintenance — 22 September 2026

The user requested removal of non-experimental accounting material from prior records and future research work. Reports and derived records have been postprocessed; affected runner and archived source copies are maintained versions rather than verbatim historical files. Token usage, request identities, bounded calls and outputs, timeouts and exclusive dispatch remain research and execution metadata.

The retained raw provider responses, generated-controller bytes, physical trajectories, HOL theories and scientific check results are the primary evidence. The maintenance verification compares their hashes against a snapshot taken before this work. It performs no model calls and does not access credentials.

Affected record and manifest checksum chains are recomputed for this maintained snapshot. These new digests do not claim to be the original pre-execution digests. The change inventory records old/new file identities without retaining removed values; it makes the transformation explicit rather than silently presenting changed archives as originals. Existing Git commit history is not rewritten.

`changes.json` records the files transformed. `verification.json` records the completed checks and any unresolved action. Earlier test/build records remain historical statements; newly executed checks are recorded separately here.

407 protected raw, prompt, source, trajectory and formal artifacts retain their pre-maintenance bytes. The core replay checks preserve all scientific fields in 60 request/record objects and reproduce 30 recorded assessments. The replication analyzer and independent generation audit reproduce the retained results. The v2 advice audit reproduces all six retained replies and source decisions; all 205 final package entries have verified path, size and SHA-256 bindings. These are offline checks, not additional experimental samples.

One exception remains: automatic approval review rejected changing the historical pilot launcher's controls. `amr-deepseek-pilot/pilot.py`, `run/snapshot-pilot.py` and `tests/test_pilot.py` are therefore unchanged and await explicit confirmation. They retain the old metadata interface and are not compatible with fresh execution against the maintained trial/smoke interface. The pilot's retained-record analysis passes independently. Current v2 and replication work does not use that legacy launcher.

The baseline hashes, transformed-file inventory, protected-byte inventory, fresh validation logs and independent review are retained here. `rebind_stages.py` documents the explicit prior-run origins used for maintained checksum inventories. Rebinding a hash is distinct from rerunning a scientific check; the verification record identifies which checks were actually rerun. Some historical log headings could not be recovered and are explicitly marked as unavailable rather than reconstructed.
