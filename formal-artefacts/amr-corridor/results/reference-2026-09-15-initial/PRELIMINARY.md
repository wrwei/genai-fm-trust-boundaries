# 开发运行，不作为最终证据入口

该运行用于开发期排查。运行过程中磁盘代码发生了完成判定修正，旧运行器尚未记录运行前后代码一致性，因此其结束时源码指纹不能代表已加载程序。两次细步长停车运行也未保存完整运动轨迹。

数值仅保留作开发记录，不与最终运行累计成独立样本。最终证据使用相邻的 `reference-2026-09-15` 目录；最终清单须确认代码和参数在运行前后不变，并为15次运行均保留运动轨迹。
