# Targeted formal-review revision

The theory now keeps Consume read-only, allowing repeated consumption of the first persistent reply. `persistent_consume` and `repeated_consume` establish that behavior. This corrects the previous mismatched clearing model; no claim remains that clearing was a concrete-mailbox overapproximation.

The broad `S_HOL` safety model is retained. New operational `source_next`/`S_source` restrict a trusted Service or Validate with empty pending to `select(core, slot)`, so that operation atomically reads and latches current mailbox advice. `source_inclusion` and `source_authority` transfer broad authority to this model. `source_latch_binding`, `source_latch_choice`, `source_advice_response`, `source_fifo_response`, `validated_choice_frozen`, and `source_provider_preserves_pending` cover latch/R-F and non-overwrite. `source_service_effect` equates every accepted source Service with its F-driven core transition. Consume is read-only bookkeeping, not the decision latch.

`authorization_from_any_pending` gives Grant within at most three Service opportunities, with arbitrary finite benign gaps and universally quantified advice at each opportunity. Any pending value is allowed initially. A valid pending choice grants immediately, empty pending needs two services, and stale/invalid pending is cleared before two more. The recursive `grants_within` predicate stops at the first Grant: free-owner/usable-permission assumptions do not continue after authorization. This remains a bounded service-pattern theorem; no infinite fairness or arbitrary-event scheduling theorem is claimed. The source-service effect lemma supplies its local operational connection. Original source bytes and Python semantics remain external finite conformance evidence.

No core transition or selector definition changed; the 11,664-row core and 1,296-row selector exports retain their previous SHA-256 values. All previous theorem names are retained. README/interface give updated scope. Final build, audit and source hashes are recorded alongside the theory. This revision supersedes the original task-2 report's mailbox-clearing and empty-pending-only limitations. Only files under `hol/` were changed during this revision.
