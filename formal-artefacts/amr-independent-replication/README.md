# Independent-initial AMR generation replication

Record-maintenance note (2026-09-22): affected records and execution copies were postprocessed at the user's request. Checksums now describe the maintained snapshot; see the [record-maintenance report](../record-maintenance-2026-09-22/README.md). Raw replies, controller source bytes and physical trajectories are retained.

Starts after research commit `8259f424d991183e1239677fb6b0de34e29cd923` was pushed to origin/main. Protocol: `literature/scholar_search_2026-09-10/followup/research_amr_replication_protocol_2026-09-17.md`.

Six new initial requests use the existing three prose variants twice (P1-R1, P2-R1, P3-R1, P1-R2, P2-R2, P3-R2), with only the previous eight-rule wording changed to twelve. Each request is a fresh context. No earlier candidate or reference solution is supplied. The fixed configuration is deepseek-flash, high thinking, 65536 max output, no temperature, nonstreaming.

All initial attempts precede repairs; at most two repairs per rejected chain and eighteen calls overall. Acceptance or same-chain program repetition stops a chain. Fatal provider/transport/usage issues stop the batch, without retry. Each request allows up to 65,536 output tokens and has 600/660 second socket/parent deadlines.

Every parseable program is assessed over the unchanged 1,824-input v1.1 obligations with the twelve-rule profile. Final content alone is source; reasoning is archived in the raw envelope but never fed back or shown in reports. Do not equate no evaluated inputs with zero violations.

The live runner exclusively creates `run/`. Never resume or delete/recreate it to redispatch the completed experiment. Credential input is confined to the guarded child. Raw provider replies, generated-controller bytes and physical trajectories from prior stages are preserved. After complete generation, each accepted chain's first accepted source is selected and exact-byte duplicates share one physical run set; every unique source gets five scenarios, with five shared handwritten calibrations. Physics requires a completed, verified live batch and separately freezes its inputs.

These are repeated requests for one task, not six independent industrial problems or a general success-rate estimate. Preference signals remain advisory under the unchanged spec. Permanent blockage need not complete; geometric simulation results do not establish hardware safety. Result reports and audits are separate from this maintained protocol overview.
