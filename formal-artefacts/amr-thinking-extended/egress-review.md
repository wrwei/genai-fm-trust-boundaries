# Concrete outbound review before extended-output calls

Maintenance note (2026-09-22): this document describes the dated experiment or review below. Test counts and review-time hashes are historical records, not claims that the current maintained files are byte-identical to the original versions. Raw provider replies, generated-controller bytes and physical trajectories are retained; current record bindings are documented in the [record-maintenance report](../record-maintenance-2026-09-22/README.md).

The user selected the DeepSeek service and model, supplied the local credential-file location, authorized the research experiments and explicitly requested larger resource allowances to finish the task. The exact proposed first requests are in `egress-preview/N64-F.request.json` and `egress-preview/T64-F.request.json`; both carry the already inspected synthetic prompt (UTF-8 request-content SHA-256 `77a084b1728c058798a2b1b941cd89a5e5f3808d4e1072b9e80dea2677528695`). The runner-generated payloads were verified equal to these previews byte for byte before dispatch. The prior stage's human-readable preview used Windows CRLF and had digest `6751e771f414ef6d19151469dc5054d3380cd82f88f232607ced3a1c35d822a3`; the prior actual request content and this stage use identical LF bytes. No historical file was changed during that outbound review.

Messages contain only the synthetic two-robot corridor specification, Boolean JSON program grammar and requirements, a prior DeepSeek-generated candidate, finite Boolean counterexamples and required outputs. They contain no paper text, real industrial data, personal information, repository inventory, credential-file contents or key value. Follow-ups use only the same specification, this condition's latest provider content and algorithmically generated finite-check feedback.

Destination is fixed to `https://api.deepseek.com/chat/completions`. The user-selected local credential is used only for normal Authorization with that service. No redirects or proxies, no automatic retries, at most eight requests. The larger output allowance implements the user's requested configuration; the previously approved data categories and recipient are unchanged.

Historical approval note: the preceding stage was initially rejected before dispatch due to hypothetical internal research data; concrete full-payload inspection established the content above and reconsideration of the same action was approved. This stage uses the same inspected first-message bytes and the same recipient, with a larger max_tokens parameter.
